export type Language = {
  id: string;
  label: string;
  nativeLabel: string;
  openAIOutputLanguageCode: string;
  shortName: string;
  themeColor: string;
  gradient: string;
  examplePhrase: string;
};

export type LanguageSide = "left" | "right";
